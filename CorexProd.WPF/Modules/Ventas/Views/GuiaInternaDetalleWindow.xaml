<Window x:Class="CorexProd.WPF.Modules.Ventas.Views.GuiaInternaDetalleWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Detalle de Guía Interna" Width="1000" Height="620" WindowStartupLocation="CenterOwner" Background="#F8FAFC">
 <Grid Margin="20"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <StackPanel><TextBlock Text="{Binding NumeroGuia,StringFormat=Guía Interna {0}}" FontSize="24" FontWeight="Bold"/><TextBlock Text="{Binding Origen,StringFormat=Origen: {0}}" Foreground="#64748B"/></StackPanel>
  <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,14,0,10"><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Almacén" Foreground="#64748B"/><TextBlock Text="{Binding NombreAlmacen}" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="Fecha" Foreground="#64748B"/><TextBlock Text="{Binding FechaEmision,StringFormat=dd/MM/yyyy}" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="OCI" Foreground="#64748B"/><TextBlock Text="{Binding NumeroOci}" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="3"><TextBlock Text="Estado" Foreground="#64748B"/><TextBlock Text="{Binding Estado}" FontWeight="SemiBold"/></StackPanel></Grid></Border>
  <Border Grid.Row="2" Background="#F0FDFA" BorderBrush="#99F6E4" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,10"><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Emitido por" Foreground="#64748B"/><TextBlock Text="{Binding UsuarioEmisor}" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="Autorizado por" Foreground="#64748B"/><TextBlock Text="{Binding UsuarioAutorizador}" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="Motivo manual" Foreground="#64748B"/><TextBlock Text="{Binding MotivoEmisionManual}" FontWeight="SemiBold" TextWrapping="Wrap"/></StackPanel></Grid></Border>
  <Border Grid.Row="3" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8"><DataGrid ItemsSource="{Binding Detalles}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="Código" Binding="{Binding CodigoProducto}" Width="110"/><DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*"/><DataGridTextColumn Header="Unidad" Binding="{Binding NombreUnidad}" Width="90"/><DataGridTextColumn Header="Cantidad despachada" Binding="{Binding CantidadDespachar,StringFormat=N2}" Width="130"/><DataGridTextColumn Header="Stock anterior" Binding="{Binding StockActual,StringFormat=N2}" Width="105"/><DataGridTextColumn Header="Observación" Binding="{Binding Observacion}" Width="180"/></DataGrid.Columns></DataGrid></Border>
  <Grid Grid.Row="4" Margin="0,12,0,0"><TextBlock Text="{Binding DetalleAnulacion}" Foreground="#BE123C" TextWrapping="Wrap"/><Button Content="Cerrar" Width="100" Height="34" HorizontalAlignment="Right" IsCancel="True"/></Grid>
 </Grid>
</Window>
