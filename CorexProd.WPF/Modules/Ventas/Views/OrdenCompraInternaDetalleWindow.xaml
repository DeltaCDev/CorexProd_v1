<Window x:Class="CorexProd.WPF.Modules.Ventas.Views.OrdenCompraInternaDetalleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalle de OCI" Width="1180" Height="680" WindowStartupLocation="CenterOwner" Background="#F3F4F6">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,14"><TextBlock Text="{Binding NumeroOci, StringFormat=Orden de Compra Interna {0}}" FontSize="23" FontWeight="Bold" Foreground="#0F172A"/><Border DockPanel.Dock="Right" CornerRadius="12" Padding="12,5"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="#DBEAFE"/><Style.Triggers><DataTrigger Binding="{Binding Estado}" Value="En proceso"><Setter Property="Background" Value="#FEF3C7"/></DataTrigger><DataTrigger Binding="{Binding Estado}" Value="Parcial"><Setter Property="Background" Value="#FFEDD5"/></DataTrigger><DataTrigger Binding="{Binding Estado}" Value="Entregado"><Setter Property="Background" Value="#DCFCE7"/></DataTrigger><DataTrigger Binding="{Binding Estado}" Value="Anulado"><Setter Property="Background" Value="#FEE2E2"/></DataTrigger></Style.Triggers></Style></Border.Style><TextBlock Text="{Binding Estado}" FontWeight="SemiBold"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="#1D4ED8"/><Style.Triggers><DataTrigger Binding="{Binding Estado}" Value="En proceso"><Setter Property="Foreground" Value="#92400E"/></DataTrigger><DataTrigger Binding="{Binding Estado}" Value="Parcial"><Setter Property="Foreground" Value="#C2410C"/></DataTrigger><DataTrigger Binding="{Binding Estado}" Value="Entregado"><Setter Property="Foreground" Value="#166534"/></DataTrigger><DataTrigger Binding="{Binding Estado}" Value="Anulado"><Setter Property="Foreground" Value="#B91C1C"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></Border></DockPanel>
        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="14" Margin="0,0,0,14">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition Width="1.6*"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel><TextBlock Text="FECHA" Foreground="#64748B" FontWeight="SemiBold"/><TextBlock Text="{Binding FechaEmision, StringFormat=dd/MM/yyyy}" Margin="0,5,0,0"/></StackPanel>
                <StackPanel Grid.Column="1"><TextBlock Text="PROFORMA" Foreground="#64748B" FontWeight="SemiBold"/><TextBlock Text="{Binding NumeroProforma}" Margin="0,5,0,0"/></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="CLIENTE" Foreground="#64748B" FontWeight="SemiBold"/><TextBlock Text="{Binding NombreCliente}" Margin="0,5,0,0" TextWrapping="Wrap"/></StackPanel>
                <StackPanel Grid.Column="3"><TextBlock Text="ORDEN DE COMPRA" Foreground="#64748B" FontWeight="SemiBold"/><TextBlock Text="{Binding OrdenCompraCliente}" Margin="0,5,0,0"/></StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="2" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="14">
            <DataGrid ItemsSource="{Binding Detalles}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Código" Binding="{Binding CodigoProducto}" Width="100"/>
                    <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*" MinWidth="260"/>
                    <DataGridTextColumn Header="Requerida" Binding="{Binding Cantidad, StringFormat=N2}" Width="90"/>
                    <DataGridTextColumn Header="Stock actual" Binding="{Binding StockActual, StringFormat=N2}" Width="90"/>
                    <DataGridTextColumn Header="Despachada" Binding="{Binding CantidadDespachada, StringFormat=N2}" Width="95"/>
                    <DataGridTextColumn Header="Pendiente" Binding="{Binding CantidadPendiente, StringFormat=N2}" Width="90"/>
                    <DataGridTextColumn Header="Estado del ítem" Binding="{Binding EstadoItem}" Width="155"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <DockPanel Grid.Row="3" Margin="0,14,0,0"><TextBlock Text="{Binding Total, StringFormat=Total: S/ {0:N2}}" FontSize="17" FontWeight="Bold" Foreground="#0F766E" VerticalAlignment="Center"/><Button DockPanel.Dock="Right" Content="Cerrar" Width="100" Height="34" Click="Cerrar_Click"/></DockPanel>
    </Grid>
</Window>
