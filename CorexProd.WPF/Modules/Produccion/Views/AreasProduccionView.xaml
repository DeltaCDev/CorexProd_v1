<UserControl x:Class="CorexProd.WPF.Modules.Produccion.Views.AreasProduccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="BotonBase" TargetType="Button">
            <Setter Property="MinWidth" Value="96"/><Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="12,0"/><Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="BotonPrimario" TargetType="Button" BasedOn="{StaticResource BotonBase}">
            <Setter Property="Background" Value="#F97316"/><Setter Property="BorderBrush" Value="#F97316"/><Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="BotonIcono" TargetType="Button">
            <Setter Property="Width" Value="30"/><Setter Property="Height" Value="30"/><Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,4,0"/><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="#CBD5E1"/>
        </Style>
        <Style x:Key="CeldaTexto" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/><Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,0"/><Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="EncabezadoCentro" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="18" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="260"/><ColumnDefinition Width="160"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Configuración de Áreas de Producción" FontSize="20" FontWeight="Bold" Foreground="#F97316"/>
                    <TextBlock Text="Define la secuencia y reglas del futuro flujo productivo" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
                <TextBox Grid.Column="1" Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="8,0" Padding="10,0" VerticalContentAlignment="Center" ToolTip="Buscar por código, nombre o descripción"/>
                <ComboBox Grid.Column="2" ItemsSource="{Binding Filtros}" SelectedItem="{Binding Filtro}" Height="36" Margin="8,0" VerticalContentAlignment="Center"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Style="{StaticResource BotonPrimario}" Command="{Binding NuevoCommand}" Margin="0,0,8,0" Content="Nueva área"/>
                    <Button Style="{StaticResource BotonBase}" Command="{Binding RefrescarCommand}" Content="Actualizar"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
            <DockPanel>
                <TextBlock DockPanel.Dock="Bottom" Text="{Binding ResumenRegistros}" FontWeight="SemiBold" Foreground="#475569" Margin="14,10"/>
                <DataGrid ItemsSource="{Binding AreasView}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          RowHeight="46" GridLinesVisibility="Horizontal" ColumnHeaderStyle="{StaticResource EncabezadoCentro}"
                          HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Acciones" Width="130">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{StaticResource BotonIcono}" ToolTip="Ver detalle" Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"><TextBlock Text="&#xE890;" FontFamily="Segoe MDL2 Assets"/></Button>
                                    <Button Style="{StaticResource BotonIcono}" ToolTip="Editar" Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"><TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" Foreground="#0369A1"/></Button>
                                    <Button Style="{StaticResource BotonIcono}" ToolTip="Activar o desactivar" Command="{Binding DataContext.CambiarEstadoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"><TextBlock Text="&#xE8AC;" FontFamily="Segoe MDL2 Assets" Foreground="#BE123C"/></Button>
                                </StackPanel>
                            </DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Código" Binding="{Binding CodigoArea}" Width="80" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Área" Binding="{Binding NombreArea}" Width="130" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="200" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Orden" Binding="{Binding OrdenSecuencia}" Width="55" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Inicio" Binding="{Binding InicioDescripcion}" Width="55" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Merma" Binding="{Binding MermaDescripcion}" Width="65" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Término" Binding="{Binding TerminoDescripcion}" Width="65" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Modo envío" Binding="{Binding ModoEnvio}" Width="85" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding EstadoDescripcion}" Width="80" ElementStyle="{StaticResource CeldaTexto}"/>
                        <DataGridTextColumn Header="Fecha registro" Binding="{Binding FechaRegistro, StringFormat=dd/MM/yyyy HH:mm}" Width="145" ElementStyle="{StaticResource CeldaTexto}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
