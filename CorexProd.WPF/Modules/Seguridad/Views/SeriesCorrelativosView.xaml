<UserControl x:Class="CorexProd.WPF.Modules.Seguridad.Views.SeriesCorrelativosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="Boton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="BotonAccion" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="IconoAccion" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Administra la numeración automática de los documentos del sistema" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox Width="250" Height="36" ItemsSource="{Binding Tipos}" DisplayMemberPath="NombreTipoDocumento" SelectedItem="{Binding TipoFiltro}"/>
                    <Button Content="Quitar filtro" Style="{StaticResource Boton}" Command="{Binding QuitarFiltroCommand}"/>
                    <Button Content="Actualizar" Style="{StaticResource Boton}" Command="{Binding ActualizarCommand}"/>
                    <Button Content="Nueva serie" Style="{StaticResource Boton}" Command="{Binding NuevoCommand}" Background="#0F766E" BorderBrush="#0F766E" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
            <DataGrid ItemsSource="{Binding Series}" SelectedItem="{Binding SerieSeleccionada}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      RowHeight="44" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Acciones" Width="95">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{StaticResource BotonAccion}" ToolTip="Editar"
                                            Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}">
                                        <TextBlock Text="&#xE70F;" Style="{StaticResource IconoAccion}" Foreground="#0369A1"/>
                                    </Button>
                                    <Button Style="{StaticResource BotonAccion}" ToolTip="Ver"
                                            Command="{Binding DataContext.VerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}">
                                        <TextBlock Text="&#xE890;" Style="{StaticResource IconoAccion}" Foreground="#111827"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Documento" Binding="{Binding NombreTipoDocumento}" Width="190"/>
                    <DataGridTextColumn Header="Serie" Binding="{Binding Serie}" Width="65"/>
                    <DataGridTextColumn Header="Último número generado" Binding="{Binding UltimoNumeroGenerado}" Width="165"/>
                    <DataGridTextColumn Header="Dígitos" Binding="{Binding CantidadDigitos}" Width="62"/>
                    <DataGridCheckBoxColumn Header="Activa" Binding="{Binding Activa}" Width="58"/>
                    <DataGridCheckBoxColumn Header="Predet." Binding="{Binding Predeterminada}" Width="60"/>
                    <DataGridTextColumn Header="Modificado por" Binding="{Binding UsuarioModificacion}" Width="115"/>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding FechaModificacion, StringFormat=dd/MM/yyyy HH:mm}" Width="125"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
