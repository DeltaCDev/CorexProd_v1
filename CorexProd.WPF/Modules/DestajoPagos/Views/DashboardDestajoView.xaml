<UserControl x:Class="CorexProd.WPF.Modules.DestajoPagos.Views.DashboardDestajoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="760" d:DesignWidth="1180"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="LabelCampo" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#64748B"/>
        </Style>

        <Style x:Key="CajaSeccion" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="KpiCaja" TargetType="Border" BasedOn="{StaticResource CajaSeccion}">
            <Setter Property="Margin" Value="0,0,10,12"/>
            <Setter Property="MinHeight" Value="76"/>
        </Style>

        <Style x:Key="BotonAccion" TargetType="Button">
            <Setter Property="MinWidth" Value="118"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E5E7EB"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E5E7EB"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderHeight" Value="34"/>
            <Setter Property="RowHeight" Value="30"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Setter Property="Foreground" Value="#111827"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderBrush" Value="#CBD5E1"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="Padding" Value="8,0"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,12,0,0">
            <Border Style="{StaticResource CajaSeccion}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Dashboard, auditoria y cierre"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#0F172A"/>
                        <TextBlock Foreground="#64748B" Margin="0,4,0,0">
                            <Run Text="Periodo: "/>
                            <Run Text="{Binding CodigoPeriodo}" FontWeight="SemiBold"/>
                            <Run Text=" | "/>
                            <Run Text="{Binding FechaInicioPeriodo, StringFormat={}{0:dd/MM/yyyy}}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding FechaFinPeriodo, StringFormat={}{0:dd/MM/yyyy}}"/>
                            <Run Text=" | Estado: "/>
                            <Run Text="{Binding EstadoPeriodo}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>

                    <WrapPanel Grid.Column="1"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center">
                        <Button Content="Crear periodo"
                                Style="{StaticResource BotonAccion}"
                                Command="{Binding CrearSemanaActualCommand}"/>
                        <Button Content="Calcular"
                                Style="{StaticResource BotonAccion}"
                                Command="{Binding CalcularPeriodoCommand}"/>
                        <Button Content="Registrar pago"
                                Style="{StaticResource BotonAccion}"
                                Command="{Binding RegistrarPagoCompletoCommand}"/>
                        <Button Content="Generar boletas"
                                Style="{StaticResource BotonAccion}"
                                Command="{Binding DescargarTodasBoletasCommand}"/>
                        <Button Content="Cerrar periodo"
                                Style="{StaticResource BotonAccion}"
                                Command="{Binding CerrarPeriodoCommand}"/>
                        <Button Content="Actualizar"
                                Style="{StaticResource BotonAccion}"
                                Command="{Binding RefrescarCommand}"/>
                    </WrapPanel>
                </Grid>
            </Border>

            <UniformGrid Columns="6">
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Trab. activos" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.TrabajadoresActivos, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Con movimientos" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.TrabajadoresConMovimientos, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Total producido" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.TotalProducido, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#166534"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Ingresos" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.TotalIngresos, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#166534"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Descuentos" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.TotalDescuentos, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#991B1B"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Neto periodo" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.NetoPeriodo, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Total pagado" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.TotalPagado, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#1D4ED8"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Saldo pendiente" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.SaldoPendiente, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#B45309"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Prestamos activos" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.PrestamosActivos, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Cuotas aplicadas" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.CuotasAplicadas, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}">
                    <StackPanel>
                        <TextBlock Text="Periodos abiertos" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.PeriodosAbiertos, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCaja}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Pend. cierre" Style="{StaticResource LabelCampo}"/>
                        <TextBlock Text="{Binding Dashboard.PeriodosPendientesCierre, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#B45309"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CajaSeccion}" Margin="0,0,10,12">
                    <StackPanel>
                        <TextBlock Text="Produccion diaria" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding ProduccionDiaria}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="170">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Dia" Binding="{Binding Etiqueta}" Width="*"/>
                                <DataGridTextColumn Header="Cant." Binding="{Binding Valor, StringFormat={}{0:N2}}" Width="80"/>
                                <DataGridTextColumn Header="Importe" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CajaSeccion}" Margin="0,0,10,12">
                    <StackPanel>
                        <TextBlock Text="Produccion por trabajador" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding ProduccionPorTrabajador}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="170">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Trabajador" Binding="{Binding Etiqueta}" Width="*"/>
                                <DataGridTextColumn Header="Cant." Binding="{Binding Valor, StringFormat={}{0:N2}}" Width="80"/>
                                <DataGridTextColumn Header="Importe" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CajaSeccion}">
                    <StackPanel>
                        <TextBlock Text="Produccion por area" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding ProduccionPorArea}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="170">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Area" Binding="{Binding Etiqueta}" Width="*"/>
                                <DataGridTextColumn Header="Cant." Binding="{Binding Valor, StringFormat={}{0:N2}}" Width="80"/>
                                <DataGridTextColumn Header="Importe" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CajaSeccion}" Margin="0,0,10,12">
                    <StackPanel>
                        <TextBlock Text="Comparativo semanal" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding ComparativoSemanal}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Semana" Binding="{Binding Etiqueta}" Width="*"/>
                                <DataGridTextColumn Header="Neto" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CajaSeccion}" Margin="0,0,10,12">
                    <StackPanel>
                        <TextBlock Text="Pagos por medio" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding PagosPorMedio}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Medio" Binding="{Binding Etiqueta}" Width="*"/>
                                <DataGridTextColumn Header="Importe" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CajaSeccion}">
                    <StackPanel>
                        <TextBlock Text="Evolucion de saldos" FontSize="15" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding EvolucionSaldos}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Periodo" Binding="{Binding Etiqueta}" Width="*"/>
                                <DataGridTextColumn Header="Saldo" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="0.9*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CajaSeccion}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Resumen por trabajador" FontSize="16" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding Resumenes}" SelectedItem="{Binding ResumenSeleccionado}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="220">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Trabajador" Binding="{Binding NombreTrabajador}" Width="180"/>
                                <DataGridTextColumn Header="Ingresos" Binding="{Binding TotalIngresos, StringFormat={}{0:N2}}" Width="90"/>
                                <DataGridTextColumn Header="Desc." Binding="{Binding TotalDescuentos, StringFormat={}{0:N2}}" Width="85"/>
                                <DataGridTextColumn Header="Pagado" Binding="{Binding TotalPagado, StringFormat={}{0:N2}}" Width="85"/>
                                <DataGridTextColumn Header="Saldo" Binding="{Binding SaldoPendiente, StringFormat={}{0:N2}}" Width="85"/>
                                <DataGridTextColumn Header="Estado" Binding="{Binding EstadoPago}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CajaSeccion}">
                    <StackPanel>
                        <TextBlock Text="Auditoria reciente" FontSize="16" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding Auditorias}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="220">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=g}" Width="115"/>
                                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="90"/>
                                <DataGridTextColumn Header="Accion" Binding="{Binding Accion}" Width="130"/>
                                <DataGridTextColumn Header="Registro" Binding="{Binding RegistroAfectado}" Width="130"/>
                                <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="160"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
