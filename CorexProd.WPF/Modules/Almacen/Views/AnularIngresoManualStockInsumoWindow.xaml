<Window x:Class="CorexProd.WPF.Modules.Almacen.Views.AnularIngresoManualStockInsumoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Anular ingreso manual"
        Width="460"
        Height="290"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F4F6">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Anular ingreso manual" FontSize="22" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock x:Name="DocumentoTextBlock" FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="16">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" FontSize="13" Foreground="#374151" Margin="0,0,0,6">
                    <Run Text="Motivo de anulacion "/><Run Text="*" Foreground="#DC2626"/>
                </TextBlock>
                <TextBox x:Name="MotivoTextBox" Height="92" MaxLength="500" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="8" VerticalContentAlignment="Top"/>
                <TextBlock x:Name="ErrorTextBlock" Foreground="#DC2626" FontSize="12" Margin="0,8,0,0" Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancelar" Width="110" Height="34" Margin="8,0,0,0" IsCancel="True"/>
            <Button Content="Confirmar" Width="110" Height="34" Margin="8,0,0,0" Background="#BE123C" BorderBrush="#BE123C" Foreground="White" Click="Confirmar_Click"/>
        </StackPanel>
    </Grid>
</Window>




